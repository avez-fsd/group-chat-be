
import { WS_CONNECTION_CLOSE_REASON } from "@constants";
import { RedisHelper } from "@helpers/redis.helper";

class ChatService {

    async sendMessage(roomId:string,  ws:any, wsServer:any) {

    }

}

export default ChatService;